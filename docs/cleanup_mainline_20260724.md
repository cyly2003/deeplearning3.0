# 2026-07-24 主线冻结、结果回拉与历史清理台账

## 主线判定

- 当前代码与结果主线固定为 **v1.2.40 `X0_molar` 三阶段全参数微调**：
  水相 pTox 预训练 → Stage-3 土壤 `neg_log10_mol_kg` 微调，随机 8:2 外层测试，
  种子 `42/2042/3407/8417`。
- 必须区分统计口径：v1.2.40 四个单模型 R2 的均值为 `0.6973`；四种子逐记录预测
  集成后在原生 `-log10(mol/kg)` 尺度上的外层测试 R2 为 `0.7155`，这是当前所称
  “约 0.71 R2 主线”的严格含义。
- v1.2.44 的锁定复现实验不替换该主线：M11U 对 v1.2.40 的四种子集成为
  `R2=0.7153, MAE=0.5447`，M10 为 `R2=0.7154, MAE=0.5458`。它们用于验证水相
  预训练贡献及 Stage-2 土壤 pTox 适配的有限增益。

## 近期结果回拉与校验

- 从 `qsar-gpu-new:/home/easyai/DL1/ecotox_qsar_transfer_git` 拉回 v1.2.52 与
  v1.2.54 的完整正式结果，而非只保留摘要。
- v1.2.52：`187` 个文件、`286,396,169` bytes；v1.2.54：`391` 个文件、
  `333,823,929` bytes。审计包分别为 `1` 个文件（`17,588` 与 `21,509` bytes）。
- 既有本机 summary 与远端同名文件逐个 SHA-256 一致；完整包已合并到
  `outputs/experiments/` 和 `outputs/audits/` 的正式 v1.2.52/v1.2.54 路径。
- 关键结果已在 `PROJECT_STATUS.md`、`docs/experiment_registry.csv`、
  `docs/experiment_decision_log.md` 与 `docs/m10_evaluation_boundary_ledger.*`
  中回填。v1.2.52 是随机五折稳健性证据，v1.2.54 支持各训练样本比例下 M10 相对
  M00 的稳健增量 MAE 收益；两者均不是 scaffold 外推指标。

## 清理结果

- 本机删除 5 个旧归档树、65 个活动旧输出目录与 16 个 v1.2.22 前的远端运行脚本；
  删除对象为 v1.2.15/18/19/21 及更早的原始运行、旧 baseline/deep 运行和 smoke。
  以文件字节和目录清单计，释放约 `39.1 GiB`，不包含当前原始 SQLite。
- 远端先确认无 QSAR 训练进程，再删除 v1.2.38/39 原始运行、v1.2.40/44/46/50/54
  smoke、v1.2.41 无胜者优化、v1.2.42 E 系列、v1.2.43 G 系列及 v1.2.49 已暂停的
  阈值诊断原始输出与审计。远端 `outputs/experiments` 由 `13G` 降至 `4.0G`，
  `outputs/audits` 由 `478M` 降至 `41M`。
- 保留 v1.2.40 正式矩阵、v1.2.44/45/46 的主线因果与稳健性证据、v1.2.47 的结构
  边界材料，以及 v1.2.50–v1.2.54 的正式结果；被清理分支的结论仍在实验登记表中
  保留，避免历史结论丢失。
